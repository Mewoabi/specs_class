static_sites:
  - name: youtube-masterclass
    buildCommand: cd my-app && pnpm install && pnpm run build
    publishPath: my-app/out
    envVars:
      - key: NODE_ENV
        value: production
      - key: NEXT_TELEMETRY_DISABLED
        value: 1

